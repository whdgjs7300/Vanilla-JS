// boolean = true or false , 0 or 1 
// 동적 ui 스위치 버튼 등 쓰임

const amiFat = true;
console.log(amiFat);

// 자바스크립트 데이터 타입 = null(값이 없음)
// 자바스크립트 데이터 타입 = undefined = 1. 변수는 있지만 값이 정의되지 않음)
// 2. 메모리 안에는 존재하지만(공간) 값이 들어가지 않음

let something;
console.log(something);
